spring:
  application:
    name: book-service
  
server:
  port: 1112

eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URI:http://13.233.122.171:8761/eureka}
  instance:
    prefer-ip-address: true
